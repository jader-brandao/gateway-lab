# Expõe o serviço app na porta do host (desenvolvimento / painel sem Caddy).
# Use: GETFY_COMPOSE_FILES=docker-compose.yml;docker-compose.hostports.yml

services:
  app:
    ports:
      - "${GETFY_HTTP_PORT:-80}:80"
